<Window x:Class="Tenurix.Management.Views.Windows.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Password"
        Width="640"
        Height="360"
        WindowStartupLocation="CenterOwner"
	Background="{StaticResource Panel}">

	<Grid Margin="20">
		<Border Background="{StaticResource Bg}"
                BorderBrush="{StaticResource Border}"
                BorderThickness="1"
                CornerRadius="16"
                Padding="20">

			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="18"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="12"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="12"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="24"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<TextBlock Text="Change Password"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="White"/>

				<TextBlock Grid.Row="1"
                           Text="Enter your current password and choose a new one."
                           Foreground="{StaticResource TextMuted}"
                           Margin="0,6,0,0"/>

				<Grid Grid.Row="3">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="170"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<TextBlock Text="Current Password" VerticalAlignment="Center" Foreground="White"/>
					<PasswordBox x:Name="CurrentPwdBox"
                                 Grid.Column="1"
                                 Height="34"
                                 Padding="10"
                                 Background="{StaticResource Panel}"
                                 Foreground="White"/>
				</Grid>

				<Grid Grid.Row="5">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="170"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<TextBlock Text="New Password" VerticalAlignment="Center" Foreground="White"/>
					<PasswordBox x:Name="NewPwdBox"
                                 Grid.Column="1"
                                 Height="34"
                                 Padding="10"
                                 Background="{StaticResource Panel}"
                                 Foreground="White"/>
				</Grid>

				<Grid Grid.Row="7">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="170"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<TextBlock Text="Confirm New" VerticalAlignment="Center" Foreground="White"/>
					<PasswordBox x:Name="ConfirmPwdBox"
                                 Grid.Column="1"
                                 Height="34"
                                 Padding="10"
                                 Background="{StaticResource Panel}"
                                 Foreground="White"/>
				</Grid>

				<StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
					<Button Content="Cancel"
                            Width="120"
                            Margin="0,0,10,0"
                            Click="Cancel_Click"/>

					<Button Content="Update Password"
                            Width="160"
                            Click="UpdatePassword_Click"/>
				</StackPanel>

			</Grid>
		</Border>
	</Grid>
</Window>
