<Window x:Class="Tenurix.Management.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tenurix Management Login"
        Width="440" Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource Bg}">

    <Window.Resources>
        <Style x:Key="LoginBtn" TargetType="Button">
            <Setter Property="Background"     Value="{StaticResource Accent}"/>
            <Setter Property="Foreground"     Value="White"/>
            <Setter Property="FontSize"       Value="14"/>
            <Setter Property="FontWeight"     Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"         Value="Hand"/>
            <Setter Property="Height"         Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{StaticResource Border}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="LoginInput" TargetType="TextBox">
            <Setter Property="Height"         Value="42"/>
            <Setter Property="Padding"        Value="12,0"/>
            <Setter Property="FontSize"       Value="14"/>
            <Setter Property="Background"     Value="{StaticResource Panel2}"/>
            <Setter Property="Foreground"     Value="{StaticResource Text}"/>
            <Setter Property="BorderBrush"    Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="LoginPwd" TargetType="PasswordBox">
            <Setter Property="Height"         Value="42"/>
            <Setter Property="Padding"        Value="12,0"/>
            <Setter Property="FontSize"       Value="14"/>
            <Setter Property="Background"     Value="{StaticResource Panel2}"/>
            <Setter Property="Foreground"     Value="{StaticResource Text}"/>
            <Setter Property="BorderBrush"    Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="40,0">

            <!-- Logo -->
            <Image Source="pack://application:,,,/Assets/Branding/tenurix_logoA.png"
                   Height="80" Stretch="Uniform" Margin="0,0,0,8"/>

            <TextBlock Text="Management Portal"
                       Foreground="{StaticResource TextMuted}" FontSize="14"
                       HorizontalAlignment="Center" Margin="0,0,0,28"/>

            <!-- Login Card -->
            <Border Background="{StaticResource Panel}" CornerRadius="16" Padding="24">
                <StackPanel>
                    <TextBlock Text="Sign In" FontSize="20" FontWeight="SemiBold"
                               Foreground="{StaticResource Text}" Margin="0,0,0,20"/>

                    <TextBlock Text="EMAIL" FontSize="10" FontWeight="SemiBold"
                               Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                    <TextBox x:Name="EmailBox" Style="{StaticResource LoginInput}"/>

                    <TextBlock Text="PASSWORD" FontSize="10" FontWeight="SemiBold"
                               Foreground="{StaticResource TextMuted}" Margin="0,16,0,6"/>
                    <PasswordBox x:Name="PasswordBox" Style="{StaticResource LoginPwd}"/>

                    <Button x:Name="LoginBtn" Content="Sign In"
                            Style="{StaticResource LoginBtn}"
                            Margin="0,24,0,0"
                            Click="Login_Click"/>

                    <TextBlock x:Name="ErrorText" Foreground="{StaticResource Danger}"
                               Margin="0,14,0,0" TextWrapping="Wrap" FontSize="13"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</Window>
