<Page x:Class="Tenurix.Management.Views.Pages.PropertySubmissionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="BorderThickness"  Value="0"/>
            <Setter Property="Padding"          Value="10,5"/>
            <Setter Property="FontSize"         Value="12"/>
            <Setter Property="FontWeight"       Value="SemiBold"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel>
            <TextBlock Text="Property Submissions" FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource Text}"/>
            <TextBlock Text="Review and manage property submissions from landlords."
                       FontSize="13" Foreground="{StaticResource TextMuted}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="2" Background="{StaticResource Panel}" CornerRadius="12" Padding="16,12">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="Refresh" Width="90" Height="36"
                        Click="Refresh_Click"
                        Style="{StaticResource ActionBtn}" Background="{StaticResource Panel2}"
                        Foreground="{StaticResource Text}"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Filter by status:" VerticalAlignment="Center"
                               Foreground="{StaticResource TextMuted}" FontSize="13" Margin="0,0,10,0"/>
                    <ComboBox x:Name="StatusFilter" Width="160" Height="36"
                              SelectionChanged="StatusFilter_SelectionChanged">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="Pending" IsSelected="True"/>
                        <ComboBoxItem Content="Approved"/>
                        <ComboBoxItem Content="Rejected"/>
                        <ComboBoxItem Content="OnHold"/>
                    </ComboBox>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Card List -->
        <Border Grid.Row="4" Background="{StaticResource Panel}" CornerRadius="14" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="Grid">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource Panel2}" CornerRadius="12"
                                    Padding="16,14" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="6"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="10"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Row 1: Address + Status badge -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Address}" FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{StaticResource Text}" TextWrapping="Wrap"/>
                                        <Border x:Name="sbadge" Grid.Column="1" CornerRadius="12" Padding="10,4"
                                                Background="#FFF7ED" VerticalAlignment="Top" Margin="10,0,0,0">
                                            <TextBlock x:Name="stxt" Text="{Binding SubmissionStatus}"
                                                       FontSize="10" FontWeight="SemiBold" Foreground="#C2410C"/>
                                        </Border>
                                    </Grid>

                                    <!-- Row 2: Landlord + Assigned info -->
                                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE77B;" FontSize="12"
                                                   Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"
                                                   Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding LandlordName}" FontSize="12"
                                                   Foreground="{StaticResource Text}" VerticalAlignment="Center"/>
                                        <TextBlock Text="  |  " FontSize="11" Foreground="{StaticResource Border}"/>
                                        <TextBlock Text="Assigned: " FontSize="11"
                                                   Foreground="{StaticResource TextMuted}"/>
                                        <TextBlock Text="{Binding AssignedToEmail, TargetNullValue='Not assigned'}"
                                                   FontSize="11" Foreground="{StaticResource Accent}"/>
                                    </StackPanel>

                                    <!-- Row 3: Action buttons -->
                                    <StackPanel Grid.Row="4" Orientation="Horizontal">
                                        <Button Content="View Details" Tag="{Binding}" Click="View_Click"
                                                Height="32" Padding="12,0" Margin="0,0,8,0"
                                                Style="{StaticResource ActionBtn}"
                                                Background="{StaticResource AccentSoft}"
                                                Foreground="{StaticResource Accent}"/>
                                        <Button Content="Assign" Tag="{Binding}" Click="Assign_Click"
                                                Height="32" Padding="12,0" Margin="0,0,8,0"
                                                Style="{StaticResource ActionBtn}"
                                                Background="{StaticResource Panel}"
                                                Foreground="{StaticResource Text}"/>
                                        <Button Content="Approve" Tag="{Binding}" Click="Approve_Click"
                                                Height="32" Padding="12,0" Margin="0,0,8,0"
                                                Visibility="{Binding CanApproveReject, Converter={StaticResource BoolToVis}}"
                                                Style="{StaticResource ActionBtn}"
                                                Background="{StaticResource SuccessSoft}"
                                                Foreground="{StaticResource Success}"/>
                                        <Button Content="Reject" Tag="{Binding}" Click="Reject_Click"
                                                Height="32" Padding="12,0"
                                                Visibility="{Binding CanApproveReject, Converter={StaticResource BoolToVis}}"
                                                Style="{StaticResource ActionBtn}"
                                                Background="{StaticResource DangerSoft}"
                                                Foreground="{StaticResource Danger}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding SubmissionStatus}" Value="Approved">
                                    <Setter TargetName="sbadge" Property="Background" Value="#F0FDF4"/>
                                    <Setter TargetName="stxt"  Property="Foreground" Value="#15803D"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SubmissionStatus}" Value="Rejected">
                                    <Setter TargetName="sbadge" Property="Background" Value="#FEF2F2"/>
                                    <Setter TargetName="stxt"  Property="Foreground" Value="#DC2626"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SubmissionStatus}" Value="OnHold">
                                    <Setter TargetName="sbadge" Property="Background" Value="#EFF6FF"/>
                                    <Setter TargetName="stxt"  Property="Foreground" Value="#1D4ED8"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

    </Grid>
</Page>
