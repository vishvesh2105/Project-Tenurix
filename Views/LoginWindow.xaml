<Window x:Class="Tenurix.Management.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tenurix Management Login"
        Width="440" Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource Bg}">

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Center" Margin="0,8,0,0">
            <Image Source="pack://application:,,,/Assets/Branding/tenurix_logoA.png"
           Height="96"
           Stretch="Uniform"
           Margin="0,0,0,10"/>
            <TextBlock Text="Management Portal"
               Foreground="{StaticResource TextMuted}"
               FontSize="14"
               HorizontalAlignment="Center"/>
        </StackPanel>


        <Border Grid.Row="2" Background="{StaticResource Panel}" CornerRadius="16" Padding="18">
            <StackPanel>
                <TextBlock Text="Email" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <TextBox x:Name="EmailBox"/>

                <TextBlock Text="Password" Foreground="{StaticResource TextMuted}" Margin="0,14,0,6"/>
                <PasswordBox x:Name="PasswordBox"/>

                <Button x:Name="LoginBtn" Content="Login"
                        Background="{StaticResource Accent}"
                        Foreground="#111827"
                        BorderThickness="0"
                        Click="Login_Click"
                        Margin="0,18,0,0"/>

                <TextBlock x:Name="ErrorText" Foreground="#FCA5A5" Margin="0,14,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
